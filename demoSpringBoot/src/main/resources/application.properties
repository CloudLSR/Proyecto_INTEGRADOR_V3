# ═══════════════════════════════════════════════════════
#  SweetCreamPink — application.properties
# ═══════════════════════════════════════════════════════

# ── Servidor ────────────────────────────────────────────
server.port=8081
 
# Base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/reposteria?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
 
# CORS — origen del frontend React
cors.allowed-origins=http://localhost:3000
 
# JWT secret (debe coincidir con JwtUtil.java)
jwt.secret=SweetCreamRoseSecretKey2024SuperSegura
jwt.expiration=86400000
jwt.reset-expiration=900000

# Logging
logging.level.root=INFO
logging.level.com.SweetCreamPink=DEBUG

# Configuración de correo (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jpsuerichard9xd@gmail.com
spring.mail.password=nfqmdvgifqlhqavn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true